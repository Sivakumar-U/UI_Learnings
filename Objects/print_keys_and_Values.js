var emp = { num: 1001, name: "Amar", salary: 35000 };

// It will print only keys from emp using Object
var keys = Object.keys(emp);
console.log(keys);

// It will print only values from emp using Object
var values = Object.values(emp);
console.log(values);
