// Map(): The map() method is used for creating a new array from an existing one, applying a function to each one of the elements of the first array.

var nums = [3, 7, 2, 8, 4, 5, 6];
var doubles = nums.map((n) => n + n);
console.log(doubles);
